const notifications = {
  notifyNewChapter: (userId, seriesId, chapterNumber) => {
    // Notify user about the new chapter for the series
  },

  sendReminder: (userId, seriesId, unreadChapters) => {
    // Send reminder to user about the unread chapters for the series
  }
};

module.exports = notifications;